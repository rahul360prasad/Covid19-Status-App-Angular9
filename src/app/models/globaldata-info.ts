// this folder and file is created only for intializing data types
// to requried data

export interface GlobalDataInfo{
  country?: string,
  confirmed?: number,
  deaths?: number,
  recovered?: number,
  active?: number,
  data?: any
}
